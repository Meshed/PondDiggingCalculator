{
  "name": "pond-digging-calculator",
  "version": "1.0.0",
  "description": "Frontend for Pond Digging Calculator - Elm application",
  "scripts": {
    "dev": "npm run build:css && parcel serve public/index.html",
    "build": "npm run build:css && parcel build public/index.html",
    "build:css": "tailwindcss -i src/styles.css -o public/styles.css --watch=false",
    "build:css:watch": "tailwindcss -i src/styles.css -o public/styles.css --watch",
    "test": "elm-test",
    "test:watch": "elm-test --watch",
    "test:e2e": "cypress run",
    "test:e2e:open": "cypress open",
    "format": "npx elm-format src/ tests/ --yes",
    "format:check": "npx elm-format src/ tests/ --validate",
    "lint": "npm run format:check",
    "pretest": "npm run format:check",
    "validate": "npm run format:check && npm run build && npm test"
  },
  "keywords": ["elm", "pond", "calculator", "excavation"],
  "author": "Pond Digging Calculator Team",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0",
    "@parcel/transformer-elm": "^2.0.0",
    "tailwindcss": "^3.4.0",
    "cypress": "^13.0.0"
  }
}